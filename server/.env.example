DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=devtool
SECRET_KEY=                # openssl rand -hex 32
FERNET_KEY=                # python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENVIRONMENT=development    # development | staging | production

# Playground lokamspace environment (seeded into env_configs via CLI)
PLAYGROUND_BASE_URL=       # e.g. https://playground.lokam.ai
PLAYGROUND_API_KEY=        # internal API key for calls-export endpoint
